import { descStyle, funcStyle } from "../styles";

export const explainCapitalize = () => {
  console.info(
    "%ccapitalize(string, info) ➡️ %creturns the string with the first letter capitalized",
    funcStyle,
    descStyle,
  );
};
